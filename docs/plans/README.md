# Design plans index

Long-form designs and decision records for the public `my` CLI, newest
first. Statuses: **active** (being implemented or next up), **shipped**
(implemented; kept for rationale), **superseded** (kept for history; the
noted successor wins on conflict).

| Plan | Status | Notes |
|---|---|---|
| [2026-08-21-first-run-bootstrap](2026-08-21-first-run-bootstrap.md) | active; candidate implemented, pending independent review and release | Stable one-line installer; resumable AUTHOR/JOIN/JOIN_REPAIR agent handoff; durable PATH; deterministic unsynced-manifest self-heal; prompt-free per-invocation GitHub HTTPS credentials; prerequisite doctor rows; observable required/optional tool readiness; explicit local completion; fail-closed conflicts. All 18 public-safe Ubuntu arm64/amd64 container walkthroughs pass. No Go/Node user prerequisite, global Git rewrite, optional-tool auto-install, policy acceptance, publication, or live-repo smoke. |
| [2026-08-14-worktree-leak-detection](2026-08-14-worktree-leak-detection.md) | shipped (v0.39.0) | Detect unregistered leftover git worktrees on umbrella mounts and catalog clones; `my session leftovers` + gated `close-worktree`; first-run docs (prereqs, WSL, produce/join manifest). No `--fix` GC, no sync hold, no launch scan. |
| [2026-08-13-grok-cursor-harness-integration](2026-08-13-grok-cursor-harness-integration.md) | shipped (v0.38.0) | Adds distinct Grok and Cursor harness adapters after live and official evidence disproved a merger. Covers native/global and launch-scoped skills, onboarding, sessions, executable collision handling, docs, and live inspection. Independent Grok review agreed. |
| [2026-07-22-policy-at-invocation](2026-07-22-policy-at-invocation.md) | shipped (v0.37.0) | Invoked bots get organization policy available at invocation plus a binding consult-when-topic-demands contract. The jointly reviewed implementation adds a compact role-scoped `## Organization Policies` guidance section, digest-verifying read actions, optional manifest `summary`/`topics`, fail-closed launch-time local digest proof, matching `policies[]` compile references, session refresh, and zero noise for non-governed organizations. |
| [2026-07-22-target-aware-gnit-publishing](2026-07-22-target-aware-gnit-publishing.md) | shipped (v0.36.0) | Fixes [#32](https://github.com/fluxinc/my-cli/issues/32): backend auto-selection is per-target roster-membership eligibility instead of umbrella-wide `.gnit` marker sniffing. Exact-member + publishable-control-root gating, fail-closed classified holds, Gnit scope preflight, dry-run parity, shared sync/session planning, and doctor partial-migration reporting were jointly designed, reviewed, and dogfooded. |
| [2026-07-21-governed-organizations-completion](2026-07-21-governed-organizations-completion.md) | complete (v0.37.0 beta) | Governance-core hardening, policy provenance, acceptance publication/reporting, trusted-branch CI, append-only supersession, record/outbox integrity, policy-at-invocation, S8c ergonomics, documentation, independent review, and sandbox plus live read-only dogfood are complete. Automatic revocation quarantine remains experimental and blocked from recommendation by its separate unmet drill gate. |
| [2026-07-16-governed-organizations](2026-07-16-governed-organizations.md) | shipped (v0.37.0 beta) | Opt-in policy, acceptance, PR publication, protected-path CI, record routing, and audit mechanisms shipped as the governance beta. Automatic quarantine remains an experimental endpoint-security plane with explicit activation and a separate disposable-repository drill gate. |
| [2026-06-30-mycli-footgun-audit](2026-06-30-mycli-footgun-audit.md) | shipped (v0.35.0) | Two-agent adversarial audit of a private operator transcript corpus. Ranks 8 confirmed-open footguns (manifest/catalog publish dead-end; #28 sync/finish dirty-base deadlock; finished-session dead-end with stale guidance; undeclared-path content never publishes; opaque `held back` status; sync won't auto-reconcile diverged mounts; no `my customers add`; duplicate role/global guidance), records already-shipped fixes that addressed recurring pain, and rejects weak findings. Implemented in this change: **manifest-scoped init follow-ups** (`internal/cli/init_test.go`), **manifest control-plane direct publish via `my publish --manifest` and sync direct** (`internal/cli/init_test.go`, `internal/cli/sync_test.go`), **#28 hold-message self-heal** (`internal/syncer/sessionhold_test.go`), **stale session diagnostics and closed-session guidance stub** (`internal/cli/work_test.go`, `internal/cli/launch_test.go`, `internal/worksession/worksession_test.go`), **handbook record publish defaults** (`internal/cli/sync_test.go`), **record write-time publish warnings** (`internal/cli/content_test.go`), **sync hold reason codes and next commands, including dirty-behind/diverged first-step guidance** (`internal/syncer/syncer_test.go`, `internal/cli/sync_test.go`, `internal/cli/doctor_test.go`), **customer record creation** (`internal/cli/content_test.go`, `internal/customers/customers_test.go`), and **duplicate guidance dedupe** (`internal/guidance/guidance_test.go`, `internal/launchplan/launchplan_test.go`, `internal/cli/launch_test.go`). |
| [2026-06-18-session-command-ergonomics](2026-06-18-session-command-ergonomics-design.md) | shipped (v0.34.0) | **Full** consolidation of the worktree work-unit on **`session`**: vocabulary, code, and on-disk layout. Adds a primary `my session start\|join\|resume\|list\|status\|finish` surface (with the new collaboration verb `my session join <id> <harness>`); keeps `my ai --new-session/--session/-r` as bot-stable launch shortcuts; demotes `my work ...` to a deprecated alias slated for removal. Renames the layout (`work/<id>` -> `sessions/<id>`, `my/work/<id>` -> `my/session/<id>`, noun-free ids) and adds a lazy, idempotent, safe **migration** for existing sessions (same-fs `git worktree move` preserves dirty work; orphans go to `my doctor`) so the `work` noun is freed everywhere. Core UX fix: creating a session surfaces id/path + "join another harness" + finish hints (stderr before exec; `--json` carries `launch_command`/`join_command`/`finish_command`). Human-short verbs, bot-wide JSON. |
| [2026-06-16-skill-rename-my-to-my-cli](2026-06-16-skill-rename-my-to-my-cli.md) | shipped (v0.33.0) | Renames the bundled public self-skill install slug from `my` to `my-cli` while keeping canonical id `my:self`; managed legacy installs migrate automatically across supported harnesses. |
| [2026-06-15-model-driven-onboarding](2026-06-15-model-driven-onboarding.md) | shipped (v0.29.0), refined | Adds model-driven onboarding: `my onboarding [--harness NAME]` launches a harness with bundled Agent-Operated Onboarding guidance; `my onboard` remains a compatibility alias. One adaptive flow (AUTHOR new org vs JOIN existing), now refined into split-pane learn-by-example command sets instead of an `OK` handshake. Onboarding teaches basic human workflows: launch harnesses, start/resume/finish sessions, check sync/doctor, and paste transcripts/raw context into harness chat for agents to operate deeper record/admin commands. CLI stays deterministic mechanism; the skill is the brain. Guardrails: command-driven mutations only, schema validation, doctor/compile gates, publish held to end, secrets never in manifest. |
| [2026-06-15-windows-cli-portability](2026-06-15-windows-cli-portability.md) | active (backlog) | Follow-up after v0.28.0 shipped Windows release builds + the Windows `my update` fix. Captures the pre-existing Windows test/portability gaps (JSON path escaping, Unix path-string expectations, `my` vs `my.exe` in tests, CLI print quoting) and proposes making the Go suite pass on Windows + a Windows CI job. Not the updater (already shipped). |
| [2026-06-14-launch-scoped-skill-composition](2026-06-14-launch-scoped-skill-composition.md) | shipped (v0.27.0) | `my ai` composes org skills into launch-root `.agents/skills` with mirrors where a launch-root seam exists; OpenCode remains compatibility-global until proven. Automatic setup/sync/doctor paths stop installing user-global org skills for launch-root-capable harnesses. Gemini harness support removed entirely (Antigravity `agy` is the replacement). Implements [ADR 0001](../decisions/0001-launch-scoped-skill-composition.md). |
| [2026-06-14-onboarding-walkthrough](2026-06-14-onboarding-walkthrough.md) | shipped (v0.26.0) | Reclaims `my onboard` as the human tour while keeping `my setup` as the deterministic machine configurator. Adds explicit `my setup --interactive`; no new top-level verbs. EOF/decline never mutate or mark; prompts loop on invalid input. |
| [2026-06-14-compile-launch-plan](2026-06-14-compile-launch-plan.md) | shipped (v0.25.0) | Mode B phase 1: `my compile` emits a deterministic manifest-to-Clawdapus launch projection. No execution, service invocation, credential resolution, descriptor fetch, or Clawdapus pod emitter in this slice. |
| [2026-06-13-data-surfaces](2026-06-13-data-surfaces.md) | active (near-term S1-S3 shipped v0.22.0-v0.24.0) | Slice 1 moved customers from manifest `catalog/customers.json` to mounted `customers/*.md` workspace records. Slice 2 adds thin **data bindings** from stable business nouns to existing `mount`/`service` primitives and removes the vestigial service `grant` field. Slice 3 adds binding-level domain notes rendered as attributed `AGENTS.md` sections. Service-backed domains remain future/YAGNI; recursive claw-pod materialization now proceeds through the Mode B compile plan. No RBAC/grants in the CLI. Supersedes the vetoed RBAC "Path B" draft. |
| [2026-06-12-contract-rules](2026-06-12-contract-rules.md) | shipped (v0.20.0-v0.21.0) | Built-in Fleet Work Contract in baseline guidance + self-skill, `my fleet get` support-record next-step hint, manifest `contract` string list rendered as `## Organization Contract` in `AGENTS.md`, plus `my contract list` and `my admin contract add|remove`. |
| [2026-06-12-cli-package-refactor](2026-06-12-cli-package-refactor.md) | shipped (v0.19.0) | `internal/cli` split into per-domain implementation and test files; `cli.go` reduced to app core/dispatcher/update shell. |
| [2026-06-12-v018-scope](2026-06-12-v018-scope.md) | shipped (v0.18.0) | Manifest `services` + `roles`, inspection verbs, `my setup --role`, umbrella-root `.mcp.json` from local/inline connection data only, doctor service checks. Session GC and Mode B compile deferred. |
| [2026-06-11-products-repos-split](2026-06-11-products-repos-split.md) | shipped (v0.15.0) | Catalog products are business entities linking repos; `catalog/repos.json` + `my repos` noun + `--repo` launch flags. Private fluxinc manifest migrated in step. |
| [2026-06-10-execution-plane](2026-06-10-execution-plane.md) | active | Mode A sessions shipped v0.14.0–v0.17.0 (opt-in launches, session-aware content commands), with later `my ai -r`/`--resume` ergonomics for launching harnesses into active sessions and concrete session guidance refreshed on resume; manifest `roles`/`services` with Mode A MCP materialization shipped in v0.18.0. Contained runners (Mode B compile) continue through the phase-1 launch projection plan. |
| [2026-06-10-single-checkout-workspace](2026-06-10-single-checkout-workspace.md) | shipped (v0.13.0) | Control/data-plane split: private manifest repo + workspace content repo, `my publish`. The file's earlier visible-single-checkout draft is superseded by its final two-repo form. |
| [2026-06-10-fleet-registry](2026-06-10-fleet-registry.md) | shipped (v0.9.0) | `my fleet` registry: one record per deployed instance, identifier resolution. |
| [2026-06-10-support-knowledgebase](2026-06-10-support-knowledgebase.md) | shipped (v0.9.0) | `my support` anonymized problem-to-solution records. |
| [2026-06-09-workspace-currency](2026-06-09-workspace-currency.md) | shipped | TTL-gated auto-refresh and stderr freshness notices for root/ai/setup. |
| [2026-06-09-self-update-design](2026-06-09-self-update-design.md) | shipped (v0.10.x) | `my update` from GitHub releases with checksum verification. |
| [2026-06-08-overarching-sync](2026-06-08-overarching-sync.md) | partially superseded | Gnit-as-spine sync design still governs the Gnit backend; its same-remote canonicalization direction is superseded by the two-repo split (the duplicate-checkout class no longer exists in the default layout). |
| [2026-06-08-flux-self-skill](2026-06-08-flux-self-skill.md) | shipped | Bundled organization-neutral `my` self-skill. |
| [2026-06-03-release-install-site](2026-06-03-release-install-site.md) | shipped | Releases, install script, docs site. |
| [2026-06-02-admin-operational-split](2026-06-02-admin-operational-split.md) | shipped | Admin vs operational command surface. |
| [2026-05-28-startup-context-ergonomics](2026-05-28-startup-context-ergonomics.md) | shipped | Startup context and guidance ergonomics. |
